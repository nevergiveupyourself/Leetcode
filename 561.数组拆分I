//方法一：排序后相加
public int arrayPairSum(int[] nums) {
        int sum=0,N=nums.length;
        Arrays.sort(nums);
        for(int i=0;i<N;i=i+2){
            sum=sum+nums[i];
        }
        return sum;
    }
